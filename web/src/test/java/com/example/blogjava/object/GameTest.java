package com.example.blogjava.object;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class GameTest {
    @DisplayName("생성")
    @Test
    void create() {
        // given
        // when
//        Game game = new Game(new Cards(Arrays.asList(1, 2, 3)));
        // then
//        assertThat(game).isNotNull();
    }

}